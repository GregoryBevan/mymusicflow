buildscript {
	repositories {
		jcenter()
	}
	
	dependencies {
        classpath build_plugins.wildfly_swarm_plugin
    }
}

plugins {
	id 'java'
	id 'maven'
}

apply plugin: "io.spring.dependency-management"

repositories {
    jcenter()
}

dependencies {
    compile libraries.slf4j
    testCompile libraries.junit
}
