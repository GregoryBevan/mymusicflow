subprojects {
	apply from: rootProject.projectDir.path + '/dependencies.gradle'

	buildscript {
		repositories {
			jcenter()
		}
	    dependencies {
	    	classpath build_plugins.gradle_bintray_plugin
        	classpath build_plugins.dependency_management_plugin
	    }
	}
}